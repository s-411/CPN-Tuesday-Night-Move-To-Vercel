# ============================================
# ENVIRONMENT VARIABLES TEMPLATE
# ============================================
# This is a template file - copy it to .env.local and fill in your values
# Command: cp .env.example .env.local
# NEVER commit .env.local to git!

# ============================================
# APPLICATION URL
# ============================================
VITE_APP_URL=http://localhost:5173

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api

VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Service role key (SECRET - only for server-side API routes)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================
# STRIPE CONFIGURATION - TEST MODE
# ============================================
# Get TEST keys from: https://dashboard.stripe.com/test/apikeys
# Get LIVE keys from: https://dashboard.stripe.com/apikeys

# Stripe publishable key
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# Stripe secret key (SECRET - only for server-side API routes)
STRIPE_SECRET_KEY=sk_test_your_secret_key

# ============================================
# STRIPE PRICE IDs
# ============================================
# Create products in Stripe Dashboard:
# https://dashboard.stripe.com/test/products (TEST mode)
# https://dashboard.stripe.com/products (LIVE mode)

# Player Mode - Weekly subscription price ID
VITE_STRIPE_PRICE_PLAYER_MODE_WEEKLY=price_your_weekly_price_id

# Player Mode - Annual subscription price ID
VITE_STRIPE_PRICE_PLAYER_MODE_ANNUAL=price_your_annual_price_id

# ============================================
# STRIPE WEBHOOK SECRET (OPTIONAL for local dev)
# ============================================
# Only needed if testing webhooks locally
# Get from: stripe listen --forward-to localhost:3000/api/stripe/webhook
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ============================================
# NOTES
# ============================================
# - Frontend variables use VITE_* prefix (required by Vite)
# - Backend variables use no prefix (server-side only)
# - For production, set these in Vercel Dashboard, not in this file
# - Use TEST mode keys for development
# - Use LIVE mode keys only in production (Vercel)
